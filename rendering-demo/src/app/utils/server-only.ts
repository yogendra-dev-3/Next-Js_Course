
import "server-only"

export function serverSideFunction(){
    console.log(`
        
        Fetching Data,
        Accessing Database
        Protecting sensitive information
        api keys`)

        return "Server Side Code"
}